# Production environment — loaded automatically by Vite during `vite build`
# This file is safe to commit: it contains no secrets, only public config.
VITE_API_BASE_URL=https://stockflow-production-3876.up.railway.app
